# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Runtime-uploaded files (App Audit screenshots) — local disk storage, not
# committed to git; must persist across deploys on whatever host serves this.
uploads/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env files (never commit secrets)
.env
.env.*
!.env.example

# Sandbox/tooling artifacts (not part of the shipped app)
.git.broken-worktree-pointer.bak
.superpowers/
.workspace/

# TypeScript incremental build info
*.tsbuildinfo
